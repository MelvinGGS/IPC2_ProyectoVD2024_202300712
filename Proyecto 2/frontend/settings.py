# ...existing code...

# Use file-based sessions
SESSION_ENGINE = 'django.contrib.sessions.backends.file'
SESSION_FILE_PATH = '/path/to/your/session/files'  # Ensure this directory exists and is writable

# ...existing code...
